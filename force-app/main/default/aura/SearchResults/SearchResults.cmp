<aura:component >
    <aura:handler event="c:SearchEvent" action="{!c.searchRecords}"/>
    <aura:attribute name="msg" type="String" 
                    default="You haven't searched for anything yet" />
    <h1>Results Component</h1>
    <div>
        {!v.msg}
    </div>
</aura:component>